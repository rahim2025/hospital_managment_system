<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="doctor_table.css"> 
    <title>All Patient List</title>
</head>
<body>
    <header>
        <h1>ALL PATIENT LIST</h1>
    </header>

    <table>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Date of Birth</th>
            <th>Gender</th>
            <th>Phone No</th>
            <th>Email</th>
            <th>Address</th>
            <th>History</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        <% for(patient of patients) { %>
            <tr>
                <td><%= patient.patient_id %></td>
                <td><%= patient.name %></td>
                <td><%= patient.date_of_birth %></td>
                <td><%= patient.gender %></td>
                <td><%= patient.phone_num %></td>
                <td><%= patient.email %></td>
                <td><%= patient.address %></td>
                <td><%= patient.history %></td>
                <td>
                    <form method="get" action="/patient/<%= patient.patient_id %>/edit">
                        <button>Edit Information</button>
                    </form>
                </td>
                <td>
                    <form method="post" action="/patient/<%= patient.patient_id %>/?_method=DELETE ">
                        <button>Delete</button>
                    </form>
                </td>
            </tr>
        <% } %>
    </table>
    <form method="get" action="/patient/add_new_patient">
        <button>Add new Patient</button>
    </form>
</body>
</html>
